"""EchoSafe backend package.

All Python source code for the platform (config, database access, ML models,
data pipelines, predictors, security, services) lives under this package so
the dashboard / Airflow / tests can all import from a single root.
"""
